{
  "name": "ac-ip",
  "version": "1.3.3",
  "dependencies": {
    "ip": "^1.1.5",
    "lodash": "4.x"
  },
  "repository": "https://github.com/mmpro/ac-ip",
  "license": "MIT",
  "devDependencies": {
    "ac-semantic-release": "^0.1.1",
    "eslint": "^7.0.0",
    "expect": "^26.0.1",
    "husky": "^4.2.3",
    "mocha": "^7.1.1"
  },
  "scripts": {
    "test": "mocha --reporter spec",
    "test-jenkins": "JUNIT_REPORT_PATH=./report.xml mocha --colors --reporter mocha-jenkins-reporter --reporter-options junit_report_name='AC IP'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "make lint-check"
    }
  }
}
